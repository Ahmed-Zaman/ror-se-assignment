<div class="container">
  <h1>New Employee</h1>
  
  <%= render 'form', 
             employee: @employee,
             url: employees_path,
             method: :post %>

  <%= link_to 'Back to Employees', employees_path, class: 'btn btn-secondary mt-3' %>
</div>
